ROUND: 41-candidate-gauntlet-2026-07-26 — LEG 5 (final grading).
Fresh grader (nt=0).

READ FIRST:
1. AGENTS.md (bedrock).
2. doc-under-review-r4.md — THE GRADED TEXT, UNCHANGED from leg 4
   (sha256 ff8b9e7a6e95239dcd1111340f4969720e526857f1746f116b42b5b405b72b05).
   The leg-4 blocking finding convicted the SCRIPT, not the
   document; only the script changed since.
3. sol-candidate-verdict-leg4.md — leg-4 register: R-A3/R-A4
   COMPLETE; R-A2 PARTIAL solely for the unverified additions
   half of the structure census (four passing addition-mutations
   listed there).
4. census-41-r5.py in this round dir == ops/census-41.py (verify
   byte-identity; sha256 687275dd612a…). RUN IT both modes + one
   wrong-digest negative.

THE SINGLE LEG-4 REPAIR (grade COMPLETE/PARTIAL/MISSING):

R-A5 (additions census verification): the script gains
verify_additions_census, wired into main and the PASS line
(which now reads "structure census (dispositions AND additions)
verified"). It verifies every addition the appendix accounts:
candidate head title; Abstract and Introduction headings; all
eight Chapter 1 section headings; the Chapter 1 seed-digest pin
parsed from the appendix, the seed file's actual digest compared
against it, AND the Chapter 1 body's byte-exact containment in
the candidate; the anchor-grade block in the seal ladder; six
committed section-17 blocks (spine, event identity, canonical
order, two tracks, bootstrap, vectors); the appendix itself.

Pre-dispatch mutation battery (all exit nonzero; original exit
zero): SOL's four leg-4 mutations (renamed Chapter 1 heading,
deleted Chapter 1 whole, renamed head, deleted Introduction) +
falsified seed pin + renamed anchor-grade block + renamed
bootstrap block + three prior-class spot checks. Run your own
battery (in-memory or restore-after-mutate with byte-restoration
verified).

PART B — the promise/behavior correspondence, final check: the
appendix's claims vs executing code, item by item. If any stated
claim still lacks a corresponding check, name it. Note the
declared scope boundary: the verifier checks presence and
byte-integrity of accounted additions; it does not (and does not
claim to) verify prose content beyond the committed markers.

PART C — VERDICT, closed menu: FIT-FOR-CEREMONY / REPAIRS-FIRST /
REARCHITECT. If FIT: the complete ceremony checklist
(SUCCESSION.md successor row; verify_kernel.py extended to the
4.1 digest; census verifier green with ceremony digest pinned;
anchor events at the gAID; ceremony facts detached; ratification
= first conformance vector) + non-blocking conditions. Single
strongest reversal consideration.

LAWS: findings-not-edits; workspace read-only except running
committed scripts (mutation via scratch/verified-restore);
user-sacrosanct spans byte-fixed; cite lines.
