# Round design — the 4.2 full-document gauntlet (2026-08-05)

Round directory: `reviews/rounds/42-3-full-gauntlet-2026-08-05/`.
Subject: `weave/custos-4.2-candidate-v2.md`, sha256
`a11f2902c6e18404ba22c9468681e8a92b57af01fe1de53b08dbabb2d5c786f0`,
3,863 lines. This is the candidate's OWN adversarial round — the
gate between repair and the ratifying authority's freeze-lift
decision. Reviewer: fresh-context, maximum effort, read-only.

## Standing law for the round

- Verify the subject digest before reading; STOP on mismatch and
  report it (a prior round caught a ratifying-authority edit this
  way — mismatch is information, not noise).
- Findings, never edits. Every finding: severity (BLOCKING /
  MAJOR / MINOR / NOTE), site (line range), the attack that
  found it, and the byte-grounds. No padding, no praise.
- The candidate's own laws bind the review: the four censuses in
  the appendix are the accounting of record; the graduated seed
  block (lines 469–844) is reviewable as CONTENT but its repairs
  route through its own station (flag, never propose in-place
  edits); confessed openness is lawful (an attack on a confessed
  boundary must show the confession is FALSE or INCOMPLETE, not
  merely that the boundary exists).
- Attack the document, not the program: prior rulings are
  context, but if ratified-record text and candidate text
  disagree, that is a FINDING regardless of what any ruling
  intended.

## Part I — the nine named attacks (adopted from the outside
## leg's verdict, per ruling S3-2; each MUST be run and scored)

The 42-2 round's outside leg cited nine findings as blocking its
ratification disposition. The repaired candidate must survive
each. For each: state the original finding (from
`reviews/rounds/42-2-integration-review-2026-08-03/sol-leg-findings.md`),
locate the repair in v2 (the repair census maps ruling → site),
and score REPAIRED / PARTIAL / UNREPAIRED with byte-grounds.

- N1 (was A4): clause-selective disclosure claimed to discharge
  the no-disclosure mandate without a presentation construction.
  Check: the three presentation forms (replay / warranted /
  proof-admitted-not-delivered) and the aggregate-Constitution
  routing note.
- N2 (was A5): issuer non-observation asserted "by type."
  Check: the scoping repair and transport/resolution surfaces.
- N3 (was A9): a universal "credential-layer salt-field
  discipline" that names nothing. Check: ACDC u-field naming,
  mandate scoping.
- N4 (was A10): Kever/Tever claimed to instantiate the
  four-valued codomain. Check: the adapter sentence.
- N5 (was A12): edge thresholds equated with signing thresholds
  as "one algebra." Check: the analogous-constructions scoping +
  forward mint clause.
- N6 (was A19): authentication flattened to one generic
  admission step. Check: KRAM naming and the cache-window
  boundary.
- N7 (was A20): witnesses claimed to discharge availability.
  Check: the charter-binds-committed-witness-state repair and
  the receipts-prove-receipt scoping.
- N8 (was A24): track-one registry reuse claimed semantically
  neutral. Check: the confession sentence and its scope.
- N9 (was A25): genus reservation treated as unilateral GEL law.
  Check: externally-stewarded-table framing, bilateral
  recognition, the seal-type namespace stated beside it.

## Part II — the standing gauntlet batteries

1. **Register battery:** BCP 14 force audit (keywords only where
   force is meant; no ALL-CAPS otherwise); economics vocabulary
   (must be absent); pulpit anaphora; participle discipline
   ("colored receipt" must not appear); direct-naming law (the
   repair claims zero unlawful generic sites outside three
   enumerated residuals — verify by independent grep, and test
   the three residuals' lawfulness claims).
2. **Sacrosanct and provenance battery:** five sacrosanct lines
   byte-exact; the seed block byte-identical to
   `weave/42-taxonomy-chapter-v2.md` body (locate by content
   anchor); abstract line 21 reads "KERI ends"; the 4.1
   predecessor digest cited correctly.
3. **Census battery:** the four censuses vs the document —
   sample 10 delta-census entries and 10 repair-census entries
   at random and verify each against the text; structure census
   rows against the actual section inventory; any change you
   find that NO census accounts is a BLOCKING finding
   (census-escape).
4. **Internal-consistency battery:** §-cross-references resolve;
   the walls enumeration (§16) is internally complete and
   matches every "wall" mention elsewhere; the codomain is
   stated identically at every site (four values + refusal
   outside, never a fifth value); the two-currents and
   no-backward-edge claims hold at every transition table; the
   three-tier fold vocabulary (Kever/Tever/Gever) used
   consistently with the adapter sentence.
5. **Deletion and substitution tests (exhibit law):** named
   instances (Utah, SEDI) carry comprehension only — delete each
   named-instance sentence and verify no normative claim loses
   its ground; substitute a counterfactual instance and verify
   no ruled span changes force.
6. **Fresh-implementer attack:** pick three normative mechanisms
   (the bundle-preimage rule; the aggregate-Constitution
   commitment; the availability floor) and attempt to implement
   each FROM THE TEXT ALONE. Every place you must guess is a
   finding: the grade of the guess (silent divergence possible =
   BLOCKING; underdetermined but fail-loud = MAJOR; cosmetic =
   MINOR).
7. **Over-generalization recheck (the 42-2 lesson, now a
   standing battery):** hunt NEW sites where a mechanism is
   written as its role — including in the prose the repair pass
   itself inserted. The repair inserted ~30 prose spans; each is
   young text that has never been reviewed.

## Part III — the supplement-3 vector obligations (spot-check tier)

Not full station fixtures (those are the 42-7 station's debt) —
but verify the TEXT supports each ruled vector's discrimination:

- S3-1: an implementer feeding edge slots to a signing-threshold
  evaluator without per-slot standing appraisal must be REFUTABLE
  from the ruled sentence alone.
- S3-3: membership-proof semantics (valid clause + salt
  recomputes; forged sibling refuses) derivable from the
  aggregate paragraph.
- S3-4: the unseated-warrantor failure mode (credential
  verification, not governance appraisal) unambiguous.
- S3-5: the floor-violating establishment event's conviction
  path derivable from KEL bytes alone per the text.
- S3-6: the delegated-gAID profile's MAY grade and confessed
  cost both present (an implementer must not read it as
  mandatory).
- S3-7: the two typed evidence objects' admissibility clear;
  the acquisition procedure's openness stated, not implied.

## Verdict

Close with: DEFECTS-FOUND (list, severity-ordered) or
FIT-FOR-FREEZE-LIFT, plus the Part I scorecard (nine rows), and
a one-paragraph disposition the ratifying authority can act on.
The freeze-lift decision itself is the authority's, never yours.

## Output

Write exactly one file:
`reviews/rounds/42-3-full-gauntlet-2026-08-05/gauntlet-report.md`.
