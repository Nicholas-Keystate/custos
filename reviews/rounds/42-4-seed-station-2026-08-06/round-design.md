# Seed-station round 42-4 — the taxonomy chapter's second regeneration (2026-08-06)

**Round directory:** `reviews/rounds/42-4-seed-station-2026-08-06/`.
**Subject:** the graduated seed `weave/42-taxonomy-chapter-v2.md`
(sha256 `dfd1ddc1a092225470d2e075c0ad7eec55a4d10e892f38501d763212fcd2bd9a`,
406 lines) → successor `weave/42-taxonomy-chapter-v3.md`.
**Authority:** the 42-3 gauntlet's seed-station flags (GF-1/N2,
SF-1, SF-2, SF-3) + supplement 3's S3-3 seed-side rulings. The
seed is graduated bytes: it repairs ONLY through this station,
by minimal byte surgery under its own law — never through the
candidate's repair passes.

## Step 0 — verify before reading

- Seed: `weave/42-taxonomy-chapter-v2.md` = `dfd1ddc1…` (above).
  STOP on mismatch.
- Gauntlet report: `reviews/rounds/42-3-full-gauntlet-2026-08-05/gauntlet-report.md`
  = sha256 `f5220ca6e5868d4dc58f1102af7cb92fce2824bcb81464b89f08bd61aec4e2f1`.
- Supplement 3: `reviews/ruling-record-supplement-3-2026-08-03.md`
  = `79c7d7bd942787c57fc4e177d1fec1424ae6e709d561e81b9ff0d860b48565cc`.

## The law of this station

1. **Minimal touch.** v3 = v2 bytes + enumerated exact-match
   substitutions (python script, exactly-once assertions), only
   at the flagged sites. Everything else byte-identical. The
   chapter's graduated status is why: each repair must be
   individually defensible at re-graduation.
2. **The sacrosanct sentence is untouchable:** the exhibit
   sentence naming Utah (the chapter's single named-instance
   site) — one sentence, USER-GATE. Locate it, verify your
   repairs never touch it.
3. **Exhibit law binds:** named instances carry comprehension
   only; deletion and substitution tests must still pass after
   repair.
4. **Register:** the chapter's own voice (substrate-first,
   declarative, confessions with force). Repairs must read as
   the chapter's prose, not as patches.
5. Seed-internal line references: the flagged sites are given in
   CANDIDATE-v2 coordinates in the gauntlet report; the seed's
   own coordinates are those minus the offset (candidate 469 =
   seed 27, i.e. seed line = candidate line − 442; VERIFY this
   offset yourself by anchor before relying on it).

## The three repairs (and no others)

### R-1 (from GF-1/N2/SF-2 — the BLOCKING): issuer
### non-observation is a surface obligation, not a type property

Site: seed lines ~363–370 ("the bar is discharged by type rather
than by audit alone … there is nothing to phone home about").
The claim SOL A5 convicted: local appraisal prevents a SEMANTIC
phone-home requirement, but does not structurally prevent
issuer observation — a verifier may query an issuer-controlled
registry, resolve issuer-published endpoints, or expose
correlatable identifiers in acquisition; an issuer-observable
presentation registry defeats the bar entirely.

Repair shape (rewrite the by-type sentence + add the surface
obligation): the type argument SURVIVES as what it actually
proves — meaning is computed at the consuming frame, so no
clause can REQUIRE issuer contact — and the residue is stated:
the bar on observation is discharged only when the verification
path itself avoids issuer-observable surfaces (evidence
acquired from non-issuer witnesses or caches; no resolution
through issuer-published endpoints during appraisal;
presentation registries, where used, holder-controlled). One
sentence of obligation, one of confession: transport-surface
observation is a deployment property, testable at the
acquisition path, and a mandate of this class binds the path,
not just the semantics.

### R-2 (from SF-1): the §2.4 conflation the S3-3 ruling convicts

Site: seed lines ~632–638 in candidate coordinates → seed
~190–196 ("A finding is disclosed instead of its evidence; a
no-disclosure mandate is discharged by construction rather than
by promise"). S3-3 ruled: revealing a finding proves the
finding was computed and committed, not that hidden evidence
satisfies the clause; the three presentation forms (replay /
warranted / proof-admitted-not-delivered) must be
distinguished; "the conflation is the defect."

Repair shape: the sentence rewrites to carry the distinction —
disclosure of a finding in place of its evidence is lawful
under one of the three presentation forms the objects section
types (replayed, warranted, or proven), and which form a
mandate accepts is the mandate's own committed choice. Cite
kernel-side (the objects section) rather than restating it —
the chapter lifts, never legislates.

### R-3 (from SF-3): the §2.8 confession list completes

Site: the chapter's closing confession list (§2.8, "What this
chapter does not claim"). Two additions, each one sentence, in
the list's existing register: (a) the observation-surface gap —
this chapter does not claim that type-level locality closes
transport-level observation; the surface obligation of §2.0
mandates is stated where the posture is defined; (b) the
presentation-form dependency — clause-selective consumption
depends on the presentation machinery the objects section
types, and this chapter's postures name WHAT is disclosed,
never HOW the disclosure is proven.

## Verification (print all)

- v3 sha256 + line count; diff stat vs v2 (changed line count
  must be small — target < 25 lines touched).
- The Utah exhibit sentence: byte-identical.
- Deletion test: remove the Utah sentence from v3 in memory —
  no normative claim loses ground (state the check's result).
- The three repairs each cite their authority in a drafting
  note in the ROUND RECORD (not in the chapter — the chapter
  stays clean prose).
- Grep: "discharged by type" must not survive; "phone home"
  must not survive unqualified; the three-presentation
  distinction present; §2.8 list grew by exactly two entries.

## Output (exactly two files)

- `weave/42-taxonomy-chapter-v3.md` (the successor seed)
- `reviews/rounds/42-4-seed-station-2026-08-06/station-record.md`
  (repairs, authorities, verification results, v3 digest —
  the re-graduation gate is the ratifying authority's read pass
  on these bytes, recorded there, not yours)
